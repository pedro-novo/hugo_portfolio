type StateObject = {
  firstName: string;
  lastName: string;
  email: string;
  selectedOption: string;
};

interface ShortcutsOption {
  column?: boolean;
}

interface VideoItemProps {
  text: string;
  videoSrc: string;
}
